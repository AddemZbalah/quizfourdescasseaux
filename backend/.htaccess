<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Quiz/backend/
    
    # Rediriger toutes les requêtes vers index.php
    RewriteRule ^index\.php$ - [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . index.php [L]
</IfModule>
